# Vendored scanner fallback

`zxing-browser.min.js` is the browser bundle from `@zxing/browser` version `0.1.5`:

https://www.npmjs.com/package/@zxing/browser/v/0.1.5

It is loaded only when the native `BarcodeDetector` API is unavailable.
